import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'molde_tela_principal',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
